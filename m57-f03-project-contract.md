<!--
文件：skills/workspace/genius-programmer/references/m57-f03-project-contract.md
编号：M57-F03
模块：M57 Workspace Skills
职责：定义索引、编号、映射、文件头、模块拆分和变更通知的统一契约。
主要规则：索引定位、完整文件阅读、文件头刷新、映射同步、README 审阅。
版权：狐一狐版权声明占位符
-->

# 项目索引与文件头规范

## 必需根文件

| 文件 | 用途 |
|---|---|
| `代码文件功能编号索引.md` | Agent 与开发者的代码导航入口 |
| `代码文件重命名映射.json` | 自然文件名与编号文件名的双向映射 |
| `正则替换.txt` | 仅供暂存构建使用的临时正则替换规则 |
| `README.md` | 面向使用者的架构、入口和快速命令 |

项目使用其他文件名时，先识别等价契约并在汇报中说明。

## 修改前通知模板

```text
准备修改：
- Mxx-Fxx path/to/file：修改原因
- Mxx-Fyy path/to/test：需要同步的测试
准备新增：
- Mxx-Fzz path/to/new-file：独立职责
同步契约：代码索引、重命名映射、README（如需要）
验证：typecheck、目标测试、build
```

通知是范围声明，不是等待批准；范围扩大时追加通知。

## 索引格式

模块使用稳定编号，文件使用功能编号：

```markdown
## M57 Workspace Skills

模块路径：[打开目录](skills/workspace)

| 代码编号 | VS Code 定位路径 | 文件类型 | 具体功能 |
|---|---|---|---|
| M57-F01 | [打开](skills/workspace/example.ps1) | 工具脚本 | 项目索引审计 |
```

- 链接目标必须是仓库根目录相对路径。
- 源码视图按 `Ctrl+左键` 打开，Markdown 预览直接点击。
- 同一路径不要同时作为标签和目标重复显示。
- 删除文件时删除索引行；迁移文件时保留映射历史。

## 重命名映射格式

```json
{
  "schemaVersion": "project.code-file-map.v1",
  "generatedAt": "2026-08-05",
  "renames": [
    {
      "id": "M57",
      "oldPath": "skills/workspace/example.ps1",
      "newPath": "skills/workspace/m57-f01-project-index-audit.ps1"
    }
  ]
}
```

- `oldPath`：自然名、历史名、框架名或构建阶段恢复名。
- `newPath`：源码工作区实际使用的编号名。
- 路径统一使用 `/`，不得逃逸项目根目录。
- 保持 `oldPath` 与 `newPath` 唯一；同一历史迁移链需要明确记录，不得猜测覆盖。

## 文件头模板

TypeScript、JavaScript、CSS、PowerShell 等支持注释的文件：

```text
文件：path/to/file
编号：Mxx-Fxx
模块：Mxx Module Name
类型：业务实现/测试/配置/脚本
职责：用一句话描述本文件边界
主要函数/类型/入口：列出最终代码中的真实符号
Agent 阅读提示：先读主要入口，再沿导入、调用和导出确认范围
版权：狐一狐版权声明占位符
```

Rust 使用 `//!` 模块文档注释。严格 JSON 不添加注释，在索引中声明版权规则。

每次修改后从最终文件重新提取函数、类型、测试场景或选择器，禁止只追加新函数而不刷新旧描述。

## 模块拆分判断

满足任一条件时优先拆为文件夹中的独立子文件：

- 有独立状态或持久化模型；
- 有独立输入/输出契约；
- 需要独立测试；
- 属于不同平台、运行时或权限边界；
- 可被多个调用方复用；
- 当前文件已混合多个不相关职责。

入口文件只负责装配、导出或框架适配。避免循环依赖，依赖方向应从入口指向实现、从 UI 指向领域接口。

## README 审阅条件

需要更新：新增顶层模块、改变运行入口、改变命令、改变安装依赖、改变构建产物或用户操作。

通常不更新：内部重命名、无外部影响的小型重构、仅测试补充。
