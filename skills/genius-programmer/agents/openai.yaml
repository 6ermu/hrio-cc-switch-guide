# 文件：skills/workspace/genius-programmer/agents/openai.yaml
# 职责：声明“天才程序员 Skill”的界面名称、默认提示与自动触发策略。
# 版权：狐一狐版权声明占位符

interface:
  display_name: "天才程序员 Skill"
  short_description: "索引驱动、可审计并支持可逆暂存构建的项目开发工作流"
  default_prompt: "Use $genius-programmer to inspect project indexes, announce the exact file plan, implement the change, refresh headers and mappings, and verify a reversible staged build."

policy:
  allow_implicit_invocation: true
